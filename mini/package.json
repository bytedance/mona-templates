{
  "name": "<%= data.projectName %>",
  "version": "1.0.0",
  "description": "Project created by mona-cli",
  "author": "unknow",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && cross-env NODE_ENV=production  mona-service build -t mini",
    "start": "rm -rf dist && cross-env NODE_ENV=development mona-service start -t mini",
    "compress": "yarn build && mona-service compress"
  },
  "dependencies": {
    "@bytedance/mona": "latest",
    "@bytedance/mona-runtime": "latest",
    "@bytedance/mona-service": "latest",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
  <%_ if(data.typescript) { _%>
    "cross-env": "^7.0.3",
    "typescript": "^4.0.2",
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.0"
  <%} else {_%>
    "cross-env": "^7.0.3"
  <%}_%>
  },
  "license": "UNLICENSED"
}