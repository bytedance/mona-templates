{
  "name": "<%= data.projectName %>",
  "version": "1.0.0",
  "description": "Project created by mona-cli",
  "author": "unknow",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && cross-env NODE_ENV=production  mona-service build -t plugin",
    "start": "rm -rf dist && cross-env NODE_ENV=development mona-service start -t plugin",
    "compress": "yarn build && mona-service compress"
  },
  "dependencies": {
    "normalize.css": "^8.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-json-view": "^1.21.3",
    "@bytedance/mona": "latest",
    "@bytedance/mona-runtime": "latest",
    "@bytedance/mona-service": "latest"
  },
  "devDependencies": {
  <%_ if(data.typescript) { _%>
    "cross-env": "^7.0.3",
    "typescript": "^4.0.2",
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.0"
  <%} else {_%>
    "cross-env": "^7.0.3"
  <%}_%>
  },
  "license": "UNLICENSED"
}