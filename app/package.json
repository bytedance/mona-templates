{
  "name": "<%= data.projectName %>",
  "version": "1.0.0",
  "description": "Project created by mona-cli",
  "author": "unknow",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && cross-env NODE_ENV=production  mona-service build",
    "start": "rm -rf dist && cross-env NODE_ENV=development mona-service start",
    "compress": "mona publish"
  },
  "dependencies": {
    "normalize.css": "^8.0.0",
    "@bytedance/mona": "0.2.0-alpha.6",
    "@bytedance/mona-runtime": "0.2.0-alpha.6",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
  <% if(data.typescript) { %>
    "cross-env": "^7.0.3",
    "typescript": "^4.0.2",
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.0"
  <%} else {%>
    "cross-env": "^7.0.3"
  <% {%>
  },
  "license": "UNLICENSED"
}