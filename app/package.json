{
  "name": "<%= data.projectName %>",
  "version": "1.0.0",
  "description": "Project created by mona-cli",
  "author": "unknow",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mona build",
    "start": "mona start",
    "compress": "mona publish"
  },
  "dependencies": {
    "normalize.css": "^8.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-router-dom": "^5.2.0",
    "@bytedance/mona": "^0.1.3",
    "@bytedance/mona-runtime": "^0.1.3"
  },<% if(data.typescript) { %>
  "devDependencies": {
      "typescript": "^4.0.2",
      "@types/react": "^17.0.0",
      "@types/react-dom": "^17.0.0"
    },<%
  } %>
  "license": "UNLICENSED"
}